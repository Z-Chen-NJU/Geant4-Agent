# RAG Placeholder (Stage 2)

Stage 1 uses JSON as the source of truth. This folder is reserved for
retrieval indexes and document chunks in Stage 2.

Expected future contents:
- `docs.jsonl`: chunked official docs
- `index/`: local index artifacts (vector or keyword)
- `retriever.py`: retrieval interface

